
open class Kunde (benutzer : String, passwort : String, var kontoNummer : Int, var guthaben : Double  ) {

}

    /*fun prämien(zahlungsBetrag : Double ): String{
        var zahlungsBetrag = warenKorb.sumOf { s: String -> String }
        if (zahlungsBetrag  < 50.00)
            return "Sie erhalten noch keine Prämie"
        if (zahlungsBetrag < 100.00)
            return "Sie erhalten einen 5€ Amazon-Gutschein"
        if (zahlungsBetrag  <150.00)
            return "Sie erhalten einen 15€ Amazon Gutschein"
        if (zahlungsBetrag < 200.00)
            return "Sie erhalten einen 25€ Amazon Gutschein"
        else
            return "Sie erhalten einen 50$ Gutschein"
    }

     */